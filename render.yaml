services:
  - type: web
    name: llm-cypher-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python Llmapi.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false